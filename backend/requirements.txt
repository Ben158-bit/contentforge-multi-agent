# ===== 运行环境 =====
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
PyYAML>=6.0.1

# ===== LLM / Agent 编排 =====
openai>=1.40.0
# 注意：langgraph 0.6.x + Python 3.10 下 interrupt 仅支持同步执行路径，
# 因此编排核心使用同步节点 + SqliteSaver（勿升级到 1.x，async interrupt 有上下文缺陷）
langgraph==0.6.11
langchain-core==0.3.71
langgraph-checkpoint-sqlite==2.0.11

# ===== Web 框架 =====
fastapi>=0.115.0
# The standard extra pulls native uvloop/httptools extensions. They are not
# needed by the test suite and can segfault on the Python 3.10 CI runner.
uvicorn>=0.30.0
python-multipart>=0.0.9

# ===== 存储 =====
# aiosqlite 0.21+ 移除了 Connection.is_alive，langgraph-checkpoint-sqlite 2.0.11 依赖它
aiosqlite==0.20.0

# ===== 测试 =====
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
